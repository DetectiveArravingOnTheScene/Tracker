name: tracker_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.1

dependencies:
  cristalyse: ^1.17.6
  drift: ^2.32.1
  drift_flutter: ^0.3.0
  path_provider: ^2.1.5
  provider: ^6.1.5+1
  flutter:
    sdk: flutter
  intl: ^0.20.2
  path: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.32.1
  build_runner: ^2.13.1

flutter:
  uses-material-design: true
